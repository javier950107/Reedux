

export const TextField = (props) =>{
    return (
        <div>
            <ul>
                <li>{props.info.name}</li>
                <li>{props.info.email}</li>
            </ul>
        </div>
    )
}